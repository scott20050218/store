<!--pages/statistics/statistics.wxml-->
<navigation-bar title="库存统计" back="{{true}}" backAsHome="{{true}}" color="black" background="#FFF"></navigation-bar>
<scroll-view class="scrollarea" scroll-y scroll-into-view="{{scrollIntoView}}" refresher-enabled="{{true}}" refresher-triggered="{{refreshing}}" bindrefresherrefresh="onPullDownRefresh">
  <view class="container">
    <!-- 出入库统计：按时间段 -->
    <view class="section-title">出入库统计</view>
    <view class="io-stats-range">
      <view class="range-item">
        <text class="range-label">开始</text>
        <picker mode="date" value="{{startDate}}" bindchange="onStartDateChange">
          <view class="range-value">{{startDate}}</view>
        </picker>
      </view>
      <view class="range-item">
        <text class="range-label">结束</text>
        <picker mode="date" value="{{endDate}}" bindchange="onEndDateChange">
          <view class="range-value">{{endDate}}</view>
        </picker>
      </view>
    </view>
    <view class="io-stats-table" wx:if="{{ioStats.length > 0}}">
      <view class="io-stats-header">
        <text class="col-item">物品</text>
        <text class="col-in">入库</text>
        <text class="col-out">出库</text>
        <text class="col-net">净变化</text>
      </view>
      <view class="io-stats-row" wx:for="{{ioStats}}" wx:key="_key">
        <view class="col-item clickable" data-item-type="{{item.itemType}}" data-mode="both" bindtap="onIODetailTap">{{item.itemType}}</view>
        <view class="col-in clickable" data-item-type="{{item.itemType}}" data-mode="inbound" bindtap="onIODetailTap">{{item.inboundQty}}</view>
        <view class="col-out clickable" data-item-type="{{item.itemType}}" data-mode="outbound" bindtap="onIODetailTap">{{item.outboundQty}}</view>
        <text class="col-net {{item.netChange > 0 ? 'net-positive' : ''}} {{item.netChange < 0 ? 'net-negative' : ''}}">{{item.netChange}}</text>
      </view>
    </view>
    <view class="io-stats-loading" wx:if="{{loading}}">
      <text class="io-stats-loading-text">加载中...</text>
    </view>
    <view class="io-stats-empty" wx:elif="{{ioStats.length === 0}}">
      <text class="io-stats-empty-text">该时间段内暂无出入库记录</text>
    </view>

    <!-- 库存明细：点击出入库统计时显示明细，否则显示库存列表 -->
    <view class="section-title" id="detailSection">
      {{detailMode ? detailTitle : '库存明细'}}
      <text class="clear-detail" wx:if="{{detailMode}}" bindtap="onClearDetail">返回</text>
    </view>
    <view class="stats-list" wx:if="{{detailMode && detailList.length > 0}}">
      <view class="detail-row {{item.tag ? '' : (item.type === 'inbound' ? 'detail-inbound' : 'detail-outbound')}}" style="{{item.tag ? 'border-left: 6rpx solid ' + item.tag : ''}}" wx:for="{{detailList}}" wx:key="_key">
        <view class="detail-left">
          <text class="detail-type">{{item.type === 'inbound' ? '入库' : '出库'}}</text>
          <text class="detail-date">{{item.date}}</text>
          <text class="detail-create-time" wx:if="{{item.createTime}}">创建时间：{{item.createTime}}</text>
          <text class="detail-user" wx:if="{{item.userName}}">操作人：{{item.userName}}</text>
          <text class="detail-location" wx:if="{{item.location}}">货架：{{item.location}}</text>
          <text class="detail-expiry" wx:if="{{item.expiryDate}}">到期：{{item.expiryDate}}</text>
        </view>
        <view class="detail-right">
          <text class="detail-qty">{{item.quantity}} {{item.displayUnit}}</text>
        </view>
      </view>
    </view>
    <view class="detail-loading" wx:elif="{{detailMode && detailLoading}}">
      <text class="detail-loading-text">加载明细中...</text>
    </view>
    <view class="empty-state" wx:elif="{{detailMode && !detailLoading && detailList.length === 0}}">
      <text class="empty-hint">该时间段内暂无记录</text>
    </view>
    <view class="empty-state" wx:elif="{{!detailMode}}">
      <text class="empty-icon">📦</text>
      <text class="empty-text">暂无库存数据</text>
      <text class="empty-hint">请先进行入库操作</text>
    </view>
  </view>
</scroll-view>
