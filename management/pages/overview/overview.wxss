/**pages/overview/overview.wxss 物品总览：物品+标签+位置+数量+入库时间+到期日期，告警样式，照片链接*/
.scrollarea {
  /* 减去导航栏高度(约44-48px+安全区)，避免总高度超出视口导致底部被裁切 */
  height: calc(100vh - 48px - constant(safe-area-inset-top));
  height: calc(100vh - 48px - env(safe-area-inset-top));
  background: #f5f5f5;
  box-sizing: border-box;
}

.container {
  padding: 30rpx;
  padding-bottom: calc(80rpx + constant(safe-area-inset-bottom));
  padding-bottom: calc(80rpx + env(safe-area-inset-bottom));
}

.list {
  background: #fff;
  border-radius: 16rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.05);
}

.item {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  padding: 28rpx 30rpx;
  border-bottom: 1rpx solid #eee;
}

.item:last-child {
  border-bottom: none;
}

.item.item-warning {
  background: #fff8f8;
  border-left: 6rpx solid #de0012;
}

.item-left {
  flex: 1;
  min-width: 0;
}

.row-name-wrap {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-bottom: 8rpx;
}

.row-name {
  font-size: 36rpx;
  font-weight: 500;
  color: #333;
}

.row-location {
  font-size: 28rpx;
  color: #666;
}

.row-tag {
  font-size: 28rpx;
  color: #666;
}

.row-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  margin-bottom: 8rpx;
}

.row-meta-item {
  font-size: 28rpx;
  color: #666;
}

.row-photo {
  margin-top: 8rpx;
}

.photo-link {
  font-size: 30rpx;
  color: #1989fa;
  text-decoration: underline;
}

.item-right {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  flex-shrink: 0;
}

.row-qty {
  font-size: 36rpx;
  font-weight: 500;
  color: #333;
  margin-bottom: 6rpx;
}

.row-alarm {
  font-size: 26rpx;
  color: #de0012;
  background: rgba(222, 0, 18, 0.1);
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
}

.empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 0;
}

.empty-icon {
  font-size: 100rpx;
  margin-bottom: 30rpx;
}

.empty-text {
  font-size: 36rpx;
  color: #666;
  margin-bottom: 10rpx;
}

.empty-hint {
  font-size: 30rpx;
  color: #999;
}
