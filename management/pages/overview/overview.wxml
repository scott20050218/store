<!--pages/overview/overview.wxml 物品总览：物品+标签+位置+数量+入库时间+到期日期，临近到期告警，有照片可点击查看-->
<navigation-bar title="物品总览" back="{{true}}" backAsHome="{{true}}" color="black" background="#FFF"></navigation-bar>
<!-- <navigation-bar title="物品总览" back="{{true}}" color="black" background="#FFF"></navigation-bar> -->
<scroll-view class="scrollarea" scroll-y refresher-enabled="{{true}}" bindrefresherrefresh="onPullDownRefresh">
  <view class="container">
    <view class="list" wx:if="{{list.length > 0}}">
      <view class="item {{item.expiryWarning ? 'item-warning' : ''}}" wx:for="{{list}}" wx:key="id">
        <view class="item-left">
          <view class="row-name-wrap">
            <text class="row-name">{{item.itemType}}</text>
            <text class="row-location" wx:if="{{item.location}}">位置：{{item.location}}</text>
            <view class="row-tag" wx:if="{{item.tag}}" style="background-color: {{item.tag}}"></view>
          </view>
          <view class="row-meta">
            <text class="row-meta-item">入库：{{item.inboundDate || '无'}}</text>
            <text class="row-meta-item">到期：{{item.expiryDate || '无'}}</text>
          </view>
          <view class="row-photo" wx:if="{{item.photoUrl}}">
            <text class="photo-link" data-url="{{item.photoUrl}}" bindtap="onPreviewPhoto">查看照片</text>
          </view>
        </view>
        <view class="item-right">
          <text class="row-qty">×{{item.quantity}}</text><text wx:if="{{item.unit}}"> {{item.unit}}</text>
          <text class="row-alarm" wx:if="{{item.expiryWarning}}">即将过期</text>
        </view>
      </view>
    </view>
    <view class="empty" wx:else>
      <text class="empty-icon">📋</text>
      <text class="empty-text">暂无物品</text>
      <text class="empty-hint">请先进行入库操作</text>
    </view>
  </view>
</scroll-view>
